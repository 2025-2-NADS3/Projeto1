<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiaClick">
        <activity
            android:name=".ActivityInicio"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityLogin" />
        <activity android:name=".ActivityCadastro" />
        <activity android:name=".ActivitySemPedido" />
        <activity android:name=".ActivityConfirmado" />
        <activity android:name=".ListaProdutosActivity"/>
        <activity android:name=".DetalhesProdutoActivity"/>
        <activity android:name=".CarrinhoActivity"/>
        <activity android:name=".ActivityMenu"/>
        <activity android:name=".PedidoConfirmado"/>
        <activity android:name=".PagamentoActivity"/>

    </application>

</manifest>